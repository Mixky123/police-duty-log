# รายงานโครงการ
## ระบบจัดการเวรและบันทึกเหตุประจำวันของตำรวจ
### (Police Duty Log System)

รายวิชา 341314 Computer Programming

---

## สารบัญ

1. [ข้อมูลโครงการ](#1-ข้อมูลโครงการ)
2. [ที่มาและความสำคัญของปัญหา](#2-ที่มาและความสำคัญของปัญหา)
3. [วัตถุประสงค์ของโปรแกรม](#3-วัตถุประสงค์ของโปรแกรม)
4. [กลุ่มผู้ใช้งานเป้าหมาย](#4-กลุ่มผู้ใช้งานเป้าหมาย)
5. [ความสามารถหลักของโปรแกรม](#5-ความสามารถหลักของโปรแกรม)
6. [เครื่องมือ ภาษาโปรแกรม และเทคโนโลยีที่ใช้](#6-เครื่องมือ-ภาษาโปรแกรม-และเทคโนโลยีที่ใช้)
7. [การออกแบบหน้าจอและกระบวนการทำงาน](#7-การออกแบบหน้าจอและกระบวนการทำงาน)
8. [โครงสร้างการเขียนโปรแกรมที่ประยุกต์ใช้](#8-โครงสร้างการเขียนโปรแกรมที่ประยุกต์ใช้)
9. [ตัวอย่างผลการทำงานของโปรแกรม](#9-ตัวอย่างผลการทำงานของโปรแกรม)
10. [ปัญหาและอุปสรรคที่พบ](#10-ปัญหาและอุปสรรคที่พบ)
11. [แนวทางการพัฒนาต่อไป](#11-แนวทางการพัฒนาต่อไป)
12. [สรุปผลการพัฒนาโปรแกรม](#12-สรุปผลการพัฒนาโปรแกรม)
13. [เอกสารอ้างอิง](#13-เอกสารอ้างอิง)

---

## 1. ข้อมูลโครงการ

**ชื่อโครงการ:** ระบบจัดการเวรและบันทึกเหตุประจำวันของตำรวจ (Police Duty Log System)

**รายชื่อสมาชิกในกลุ่มและหน้าที่รับผิดชอบ:**

*(กรุณาเพิ่มรายชื่อสมาชิกและหน้าที่ของแต่ละคนที่นี่)*

| ลำดับ | ชื่อ-สกุล | รหัสนักศึกษา | หน้าที่รับผิดชอบ |
|-------|-----------|--------------|-----------------|
| 1 | *(ชื่อสมาชิก 1)* | *(รหัส)* | ออกแบบระบบ, พัฒนาส่วน Backend (app.py, db.py) |
| 2 | *(ชื่อสมาชิก 2)* | *(รหัส)* | ออกแบบ UI/UX, พัฒนาส่วน Frontend (HTML, CSS) |
| 3 | *(ชื่อสมาชิก 3)* | *(รหัส)* | ทดสอบระบบ, เขียนเอกสาร, เตรียม deploy |
| ... | ... | ... | ... |

**วันที่เริ่มโครงการ:** *(ระบุวันที่)*

**วันที่เสร็จสิ้น:** *(ระบุวันที่)*

---

## 2. ที่มาและความสำคัญของปัญหา

ในการปฏิบัติงานของหน่วยงานตำรวจ การจัดการข้อมูลเจ้าหน้าที่ ตารางเวรประจำวัน และการบันทึกเหตุการณ์ที่เกิดขึ้นในพื้นที่รับผิดชอบ เป็นงานที่สำคัญและต้องทำอย่างต่อเนื่อง การบันทึกข้อมูลด้วยกระดาษหรือสมุดบันทึกแบบดั้งเดิมมีข้อจำกัดหลายประการ เช่น

- **การค้นหาข้อมูลย้อนหลังทำได้ยาก** เมื่อต้องการตรวจสอบเหตุการณ์หรือตารางเวรในอดีต
- **ข้อมูลกระจัดกระจาย** ไม่สามารถเห็นภาพรวมหรือสรุปสถิติได้ทันที
- **เสี่ยงต่อการสูญหาย** กระดาษอาจชำรุดหรือหายได้
- **การแบ่งปันข้อมูลระหว่างเวรไม่สะดวก** ต้องส่งต่อกันด้วยตนเอง

ระบบจัดการเวรและบันทึกเหตุประจำวันแบบดิจิทัลจึงเป็นทางแก้ปัญหาที่ช่วยให้การทำงานมีประสิทธิภาพมากขึ้น สามารถเข้าถึงข้อมูลได้ทันที มีระบบค้นหาที่รวดเร็ว และสรุปภาพรวมเชิงสถิติได้อัตโนมัติ นอกจากนี้ยังสามารถนำไปใช้เป็นหลักฐานในการรายงานผลการปฏิบัติงานต่อผู้บังคับบัญชาได้อีกด้วย

---

## 3. วัตถุประสงค์ของโปรแกรม

1. เพื่อพัฒนาระบบจัดการข้อมูลเจ้าหน้าที่ตำรวจให้สามารถเพิ่ม แก้ไข ลบ และค้นหาข้อมูลได้อย่างมีประสิทธิภาพ
2. เพื่อพัฒนาระบบจัดตารางเวรประจำวันที่สามารถมอบหมายงานและติดตามสถานะการปฏิบัติงานได้แบบเรียลไทม์
3. เพื่อสร้างระบบบันทึกเหตุการณ์ประจำวันที่จัดเก็บข้อมูลเหตุการณ์ตามประเภท ระดับความรุนแรง และสถานะการดำเนินการได้อย่างเป็นระบบ
4. เพื่อพัฒนาหน้าแดshบอร์ดที่แสดงสรุปภาพรวมและสถิติการทำงานในรูปแบบที่เข้าใจง่าย
5. เพื่อประยุกต์ใช้ความรู้ด้านการเขียนโปรแกรมจากรายวิชา 341314 Computer Programming ในการพัฒนาระบบที่สามารถใช้งานได้จริง

---

## 4. กลุ่มผู้ใช้งานเป้าหมาย

- **เจ้าหน้าที่ตำรวจระดับปฏิบัติการ** ใช้บันทึกเหตุการณ์ที่พบเจอในแต่ละเวร
- **ผู้บังคับบัญชาระดับหัวหน้างาน** ใช้จัดตารางเวร ตรวจสอบสถานะการปฏิบัติงาน และดูภาพรวมสถิติเหตุการณ์
- **เจ้าหน้าที่สารบรรณ/ธุรการ** ใช้จัดการข้อมูลเจ้าหน้าที่และสรุปรายงานประจำวัน
- **ผู้บริหารหน่วยงาน** ใช้ดูสถิติภาพรวมเพื่อประกอบการตัดสินใจวางแผนงานและจัดสรรทรัพยากร

---

## 5. ความสามารถหลักของโปรแกรม

### 5.1 ระบบสมาชิกและการยืนยันตัวตน
- สมัครบัญชีผู้ใช้ใหม่พร้อมตรวจสอบรหัสผ่านยืนยัน
- เข้าสู่ระบบด้วย username และ password
- รหัสผ่านถูกเข้ารหัสด้วย SHA-256 ก่อนบันทึกลงฐานข้อมูล (ไม่เก็บเป็น plain text)
- ป้องกันหน้าที่ต้องล็อกอินก่อนใช้งานด้วย decorator `@login_required`
- ออกจากระบบเพื่อล้าง session

### 5.2 จัดการข้อมูลเจ้าหน้าที่ (Officers Management)
- **เพิ่ม** เจ้าหน้าที่ใหม่ โดยระบุ ยศ ชื่อ-สกุล เลขประจำตัว (Badge No.) เบอร์โทร และสังกัด
- **แก้ไข** ข้อมูลเจ้าหน้าที่ที่มีอยู่แล้ว
- **ลบ** เจ้าหน้าที่ออกจากระบบ
- **แสดงรายชื่อ** เจ้าหน้าที่ทั้งหมดในตารางพร้อมปุ่มดำเนินการ
- ตรวจสอบเลขประจำตัวห้ามซ้ำ (UNIQUE constraint)

### 5.3 จัดตารางเวร (Duty Scheduling)
- **จัดเวร** โดยเลือกเจ้าหน้าที่ ระบุวันที่ ผลัด (เช้า/บ่าย/ดึก) สถานที่ปฏิบัติงาน และหมายเหตุ
- **อัปเดตสถานะเวร** เป็น "รอเข้าเวร (scheduled)" / "กำลังปฏิบัติ (on-duty)" / "เสร็จสิ้น (completed)"
- **ลบรายการเวร** ที่ไม่ต้องการ
- แสดงตารางเวรทั้งหมดพร้อมชื่อเจ้าหน้าที่ผู้รับผิดชอบ (ใช้ SQL JOIN)

### 5.4 บันทึกเหตุประจำวัน (Incident Logging)
- **บันทึกเหตุการณ์** โดยระบุ เวลา ประเภท (อุบัติเหตุ/ลักทรัพย์/ทะเลาะ/ยาเสพติด/ฯลฯ) ระดับความรุนแรง (ต่ำ/ปานกลาง/สูง/วิกฤต) สถานที่ รายละเอียด และเจ้าหน้าที่ผู้รับผิดชอบ
- **อัปเดตสถานะเหตุการณ์** เป็น "เปิดอยู่ (open)" / "กำลังดำเนินการ (in-progress)" / "ปิดแล้ว (closed)"
- **ลบบันทึกเหตุการณ์**
- แสดงบันทึกทั้งหมดเรียงตามเวลาล่าสุด (ORDER BY DESC)

### 5.5 แดชบอร์ดสรุปภาพรวม (Dashboard)
- แสดงตัวเลขสรุป 4 ช่อง: จำนวนเจ้าหน้าที่ / จำนวนเวร / จำนวนเหตุการณ์ทั้งหมด / เหตุที่ยังไม่ปิด
- กราฟแท่งแสดงจำนวนเหตุการณ์แยกตามประเภท (ใช้ SQL GROUP BY และ CSS สำหรับแท่ง)
- สรุปจำนวนเหตุการณ์ตามระดับความรุนแรง

---

## 6. เครื่องมือ ภาษาโปรแกรม และเทคโนโลยีที่ใช้

### 6.1 ภาษาและเฟรมเวิร์ก
- **Python 3** — ภาษาโปรแกรมหลัก
- **Flask 3.1.3** — เว็บเฟรมเวิร์กสำหรับสร้าง web application พร้อม routing, session management, และ template engine (Jinja2)

### 6.2 ฐานข้อมูล
- **SQLite** — ใช้ระหว่างพัฒนาในเครื่อง (ไม่ต้องติดตั้งเซิร์ฟเวอร์)
- **PostgreSQL** — ใช้เมื่อ deploy ขึ้นออนไลน์ (มีความเสถียรและรองรับผู้ใช้พร้อมกันได้มากกว่า)
- ระบบตรวจว่ามี environment variable `DATABASE_URL` หรือไม่ แล้วเลือกฐานข้อมูลอัตโนมัติ ทำให้ใช้ซอร์สโค้ดชุดเดียวได้ทั้งสองสภาพแวดล้อม

### 6.3 ส่วนติดต่อผู้ใช้ (Frontend)
- **HTML5** — โครงสร้างหน้าเว็บ
- **CSS3** — ออกแบบสไตล์และเลย์เอาต์ (ไม่ใช้ framework เพื่อควบคุมการออกแบบเองทั้งหมด)
- **Jinja2 Template Engine** — สร้างหน้าเว็บแบบ dynamic โดยใส่ข้อมูลจาก Python ลงใน HTML

### 6.4 เว็บเซิร์ฟเวอร์และการ Deploy
- **gunicorn 23.0.0** — WSGI server สำหรับรันแอป Flask บนเซิร์ฟเวอร์จริง (รองรับ production workload)
- **Render** — แพลตฟอร์ม cloud hosting ที่รองรับ deploy ฟรีพร้อมฐานข้อมูล PostgreSQL
- **render.yaml** — ไฟล์กำหนดค่า infrastructure-as-code สำหรับ deploy อัตโนมัติ

### 6.5 เครื่องมืออื่น ๆ
- **Git** — ระบบควบคุมเวอร์ชันของโค้ด
- **GitHub** — เก็บซอร์สโค้ดและเชื่อมกับ Render เพื่อ auto-deploy

---

## 7. การออกแบบหน้าจอและกระบวนการทำงาน

### 7.1 โครงสร้างการนำทาง (Navigation)
ระบบใช้แถบเมนูด้านบนแบบคงที่ (navbar) ที่แสดงเมนูหลัก 4 หน้า:
- **แดชบอร์ด** — หน้าสรุปภาพรวม
- **เจ้าหน้าที่** — จัดการข้อมูลเจ้าหน้าที่
- **ตารางเวร** — จัดตารางเวรและติดตามสถานะ
- **บันทึกเหตุ** — บันทึกเหตุการณ์ประจำวัน

มุมขวาบนแสดงชื่อผู้ใช้ที่เข้าสู่ระบบและปุ่ม "ออกจากระบบ"

### 7.2 ลำดับการทำงาน (Workflow)
```
[เข้าสู่ระบบ] 
    ↓
[แดชบอร์ด] ← ภาพรวมสถิติ
    ↓
เลือกเมนู → [เจ้าหน้าที่] → เพิ่ม/แก้ไข/ลบข้อมูลเจ้าหน้าที่
           → [ตารางเวร]  → จัดเวร อัปเดตสถานะ
           → [บันทึกเหตุ] → บันทึกเหตุการณ์ อัปเดตสถานะคดี
```

### 7.3 หน้าจอหลัก

**หน้าเข้าสู่ระบบ (login.html)**
- ฟอร์มกรอก Username และ Password
- ปุ่ม "เข้าสู่ระบบ" และลิงก์ "สมัครสมาชิก"
- แสดงข้อความแจ้งเตือนเมื่อล็อกอินไม่สำเร็จ

**หน้าแดชบอร์ด (dashboard.html)**
- 4 กล่องสถิติหลัก แสดงจำนวนเจ้าหน้าที่ เวร เหตุการณ์ และเหตุที่เปิดอยู่
- กราฟแท่งแนวนอนสรุปเหตุการณ์ตามประเภท (ใช้ CSS สร้างแท่งตามสัดส่วน)

**หน้าเจ้าหน้าที่ (officers.html)**
- ฟอร์มเพิ่มเจ้าหน้าที่ใหม่ด้านบน
- ตารางแสดงรายชื่อทั้งหมด แต่ละแถวมีปุ่ม "แก้ไข" และ "ลบ"

**หน้าตารางเวร (duties.html)**
- ฟอร์มจัดเวรด้านบน เลือกเจ้าหน้าที่จาก dropdown
- ตารางแสดงเวรทั้งหมดพร้อมสถานะและปุ่มอัปเดต/ลบ

**หน้าบันทึกเหตุ (incidents.html)**
- ฟอร์มบันทึกเหตุการณ์ใหม่ด้านบน
- ตารางแสดงเหตุการณ์ทั้งหมดเรียงตามเวลาล่าสุดก่อน พร้อมปุ่มอัปเดตสถานะ/ลบ

---

## 8. โครงสร้างการเขียนโปรแกรมที่ประยุกต์ใช้

โปรแกรมนี้ใช้โครงสร้างการเขียนโปรแกรมที่เรียนในรายวิชา 341314 Computer Programming ดังนี้

### 8.1 ตัวแปรและชนิดข้อมูล (Variables & Data Types)
- ใช้ตัวแปร string, integer เก็บข้อมูลจากฟอร์ม เช่น `username`, `password`, `officer_id`
- ใช้ dictionary เก็บข้อมูลผู้ใช้และผลลัพธ์จากฐานข้อมูล
- ใช้ list เก็บรายการเจ้าหน้าที่ ตารางเวร และเหตุการณ์ทั้งหมด

**ตัวอย่างโค้ด (app.py)**
```python
username = request.form.get("username", "").strip()
password = request.form.get("password", "").strip()
user = db.verify_login(username, password)  # คืนค่าเป็น dict
```

### 8.2 เงื่อนไข (Conditional Statements)
- ใช้ `if/else` ตรวจสอบว่าผู้ใช้ล็อกอินสำเร็จหรือไม่
- ใช้ `if` ตรวจสอบความครบถ้วนของข้อมูลก่อนบันทึก
- ใช้ `if` เลือกฐานข้อมูลตามสภาพแวดล้อม (SQLite หรือ PostgreSQL)

**ตัวอย่างโค้ด (db.py)**
```python
if USE_POSTGRES:
    import psycopg2
    # ใช้ PostgreSQL
else:
    import sqlite3
    # ใช้ SQLite
```

### 8.3 การวนซ้ำ (Loops)
- ใช้ `for` loop วนดึงผลลัพธ์จากฐานข้อมูล แปลงเป็น list ของ dict
- ใช้ `for` loop ใน template (Jinja2) แสดงตารางข้อมูล
- ใช้ `for` loop สรุปสถิติตามประเภท

**ตัวอย่างโค้ด (db.py)**
```python
cur.execute("SELECT category, COUNT(*) FROM incidents GROUP BY category;")
by_category = {}
for row in cur.fetchall():
    by_category[row[0]] = row[1]
```

### 8.4 ฟังก์ชัน (Functions)
- แยกหน้าที่การทำงานเป็นฟังก์ชันย่อย ๆ เช่น `verify_login()`, `add_officer()`, `get_all_duties()`
- ใช้ decorator `@login_required` เป็นฟังก์ชันห่อหุ้มฟังก์ชันอื่น (higher-order function)
- ฟังก์ชันคืนค่าเป็น tuple `(success, message)` สำหรับแจ้งผลการทำงาน

**ตัวอย่างโค้ด (db.py)**
```python
def add_officer(rank, full_name, badge_no, phone, station):
    if not rank or not full_name or not badge_no:
        return False, "กรุณากรอกข้อมูลให้ครบ"
    # ... บันทึกลงฐานข้อมูล
    return True, "บันทึกข้อมูลเจ้าหน้าที่สำเร็จ"
```

### 8.5 โครงสร้างข้อมูลพื้นฐาน (Data Structures)
- ใช้ **list** เก็บรายการข้อมูลหลายรายการ เช่น `all_officers`, `all_duties`
- ใช้ **dictionary** เก็บข้อมูลแต่ละแถวจากฐานข้อมูล เพื่อเข้าถึงด้วยชื่อคอลัมน์

**ตัวอย่างโค้ด (db.py)**
```python
def get_all_officers():
    # คืนค่าเป็น list ของ dict
    cur.execute("SELECT * FROM officers ORDER BY id;")
    rows = [_row_to_dict(r) for r in cur.fetchall()]
    return rows
```

### 8.6 การรับข้อมูลและแสดงผล (Input/Output)
- ใช้ `request.form.get()` รับข้อมูลจากฟอร์ม HTML
- ใช้ `render_template()` ส่งข้อมูลไปแสดงใน template
- ใช้ `flash()` แสดงข้อความแจ้งเตือนให้ผู้ใช้

**ตัวอย่างโค้ด (app.py)**
```python
rank = request.form.get("rank", "").strip()
success, msg = db.add_officer(rank, full_name, badge_no, phone, station)
flash(msg, "success" if success else "danger")
```

### 8.7 การจัดการข้อมูลในฐานข้อมูล (Database Operations)
- ใช้ SQL สร้างตาราง (CREATE TABLE)
- ใช้ SQL เพิ่มข้อมูล (INSERT)
- ใช้ SQL ดึงข้อมูล (SELECT) พร้อม JOIN เชื่อมตาราง
- ใช้ SQL อัปเดตข้อมูล (UPDATE)
- ใช้ SQL ลบข้อมูล (DELETE)
- ใช้ SQL สรุปสถิติ (COUNT, GROUP BY, ORDER BY)

**ตัวอย่างโค้ด (db.py)**
```python
cur.execute("""
    SELECT d.*, o.rank, o.full_name 
    FROM duties d
    JOIN officers o ON d.officer_id = o.id
    ORDER BY d.duty_date DESC;
""")
```

### 8.8 การจัดการข้อผิดพลาด (Exception Handling)
- ใช้ `try/except` จับข้อผิดพลาดจากฐานข้อมูล เช่น ข้อมูลซ้ำ (UNIQUE constraint)
- แสดงข้อความที่เป็นมิตรกับผู้ใช้แทนที่จะให้โปรแกรมหยุดทำงาน

**ตัวอย่างโค้ด (db.py)**
```python
try:
    cur.execute("INSERT INTO officers ...")
    conn.commit()
    return True, "บันทึกสำเร็จ"
except Exception as e:
    if "unique" in str(e).lower():
        return False, "เลขประจำตัวนี้มีอยู่แล้ว"
    return False, "เกิดข้อผิดพลาด: " + str(e)
```

---

## 9. ตัวอย่างผลการทำงานของโปรแกรม

### 9.1 หน้าเข้าสู่ระบบ
- ผู้ใช้กรอก username และ password
- กดปุ่ม "เข้าสู่ระบบ"
- ถ้าข้อมูลถูกต้อง จะเข้าสู่หน้าแดชบอร์ด พร้อมข้อความต้อนรับ
- ถ้าข้อมูลผิด จะแจ้ง "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง"

### 9.2 หน้าแดชบอร์ด
แสดงตัวเลขสรุป เช่น:
- **จำนวนเจ้าหน้าที่:** 6 นาย
- **จำนวนเวรทั้งหมด:** 6 รายการ
- **จำนวนเหตุการณ์:** 8 รายการ
- **เหตุที่ยังไม่ปิด:** 5 รายการ

แสดงกราฟแท่งสรุปตามประเภท เช่น:
- ลักทรัพย์: 2 ครั้ง
- อุบัติเหตุจราจร: 2 ครั้ง
- ทะเลาะวิวาท: 1 ครั้ง
- ยาเสพติด: 1 ครั้ง

### 9.3 หน้าจัดการเจ้าหน้าที่
เมื่อเพิ่มเจ้าหน้าที่ใหม่:
```
ยศ: พ.ต.ท.
ชื่อ-สกุล: สมชาย ใจเด็ด
เลขประจำตัว: P1001
เบอร์โทร: 081-111-1111
สังกัด: สภ.เมือง
```
กดปุ่ม "เพิ่มเจ้าหน้าที่" → แสดงข้อความ "บันทึกข้อมูลเจ้าหน้าที่สำเร็จ"

ถ้ากรอกเลขประจำตัวซ้ำ → แสดง "เลขประจำตัว (Badge No.) นี้มีอยู่แล้วในระบบ"

### 9.4 หน้าจัดตารางเวร
เมื่อจัดเวร:
```
เลือกเจ้าหน้าที่: พ.ต.ท. สมชาย ใจเด็ด
วันที่: 2026-06-05
ผลัด: ผลัดเช้า (08:00-16:00)
สถานที่: ป้อมตำรวจสี่แยกกลางเมือง
หมายเหตุ: ดูแลการจราจรช่วงเช้า
```
กดบันทึก → แสดง "บันทึกตารางเวรสำเร็จ"

สามารถอัปเดตสถานะจาก "รอเข้าเวร" → "กำลังปฏิบัติ" → "เสร็จสิ้น" ได้

### 9.5 หน้าบันทึกเหตุประจำวัน
เมื่อบันทึกเหตุ:
```
เวลาเกิดเหตุ: 2026-06-05 09:30
ประเภท: อุบัติเหตุจราจร
ระดับความรุนแรง: ปานกลาง
สถานที่: สี่แยกกลางเมือง
รายละเอียด: รถจักรยานยนต์ชนกับรถยนต์ มีผู้บาดเจ็บเล็กน้อย 1 ราย
ผู้รับผิดชอบ: พ.ต.ท. สมชาย ใจเด็ด
```
กดบันทึก → แสดง "บันทึกเหตุการณ์สำเร็จ"

สามารถอัปเดตสถานะจาก "เปิดอยู่" → "กำลังดำเนินการ" → "ปิดแล้ว" ได้

---

## 10. ปัญหาและอุปสรรคที่พบ

### 10.1 การรองรับฐานข้อมูลสองแบบ
**ปัญหา:** SQLite และ PostgreSQL มี syntax ที่แตกต่างกันเล็กน้อย เช่น
- SQLite ใช้ `?` สำหรับ placeholder, PostgreSQL ใช้ `%s`
- SQLite ใช้ `INTEGER PRIMARY KEY AUTOINCREMENT`, PostgreSQL ใช้ `SERIAL PRIMARY KEY`

**วิธีแก้:** สร้างตัวแปร `PH` (placeholder) และ `pk` (primary key definition) ที่ปรับตามฐานข้อมูลที่ใช้ แล้วแทรกลงใน SQL string ด้วย f-string

### 10.2 การเข้ารหัสรหัสผ่าน
**ปัญหา:** เก็บรหัสผ่านเป็น plain text ไม่ปลอดภัย แต่การใช้ bcrypt ต้องติดตั้ง library เพิ่ม

**วิธีแก้:** ใช้ SHA-256 จาก `hashlib` ที่มีใน Python standard library อยู่แล้ว (ไม่ต้องติดตั้งเพิ่ม) เข้ารหัสรหัสผ่านก่อนบันทึกและตอนตรวจสอบ

### 10.3 การจัดการ Session และ Redirect
**ปัญหา:** บางหน้าต้องเข้าสู่ระบบก่อน แต่เขียน if-check ซ้ำทุกฟังก์ชันจะซ้ำซาก

**วิธีแก้:** สร้าง decorator `@login_required` แล้วใส่หน้าฟังก์ชันที่ต้องป้องกัน ทำให้โค้ดสะอาดและใช้ซ้ำได้

### 10.4 การแสดงกราฟแท่งโดยไม่ใช้ JavaScript
**ปัญหา:** อยากได้กราฟแท่งแบบง่าย แต่ไม่อยากพึ่ง chart library (Chart.js, D3) เพราะต้อง load script เพิ่ม

**วิธีแก้:** ใช้ CSS สร้างแท่งด้วย `width` ที่คำนวณจากสัดส่วนข้อมูล และใช้ Jinja2 loop สร้าง HTML สำหรับแต่ละแท่ง

### 10.5 การ Deploy บน Render
**ปัญหา:** ครั้งแรกที่ push ขึ้น Render build ล้มเหลวเพราะไม่มี `psycopg2`

**วิธีแก้:** เพิ่ม `psycopg2-binary` ลงใน `requirements.txt` และตรวจสอบว่า `Procfile` มีคำสั่ง `gunicorn app:app` ถูกต้อง

---

## 11. แนวทางการพัฒนาต่อไป

1. **เพิ่มการค้นหาและกรองข้อมูล** 
   - ค้นหาเจ้าหน้าที่ตามชื่อหรือเลขประจำตัว
   - กรองเหตุการณ์ตามช่วงวันที่ ประเภท หรือระดับความรุนแรง

2. **เพิ่มสิทธิ์ผู้ใช้ (Role-Based Access Control)**
   - ผู้ดูแล (admin) สามารถจัดการทุกอย่าง
   - เจ้าหน้าที่ระดับปฏิบัติการ (officer) ดูและบันทึกเหตุได้อย่างเดียว

3. **ส่งออกรายงานเป็นไฟล์ PDF หรือ Excel**
   - ใช้ library เช่น `reportlab` หรือ `openpyxl`

4. **เพิ่มการแจ้งเตือน (Notification)**
   - แจ้งเตือนเมื่อใกล้ถึงเวลาเข้าเวร
   - แจ้งเมื่อมีเหตุการณ์ใหม่ที่มีระดับความรุนแรงสูง

5. **เพิ่มการอัปโหลดรูปภาพหรือไฟล์แนบ**
   - อัปโหลดรูปหน้างาน รูปที่เกิดเหตุ หรือเอกสารประกอบ

6. **ปรับปรุง UI/UX**
   - เพิ่มการแสดงผลแบบ responsive สำหรับจอมือถือ
   - ใช้ JavaScript เพิ่ม interactivity เช่น modal แทนการ redirect

7. **เพิ่มกราฟและสถิติขั้นสูง**
   - กราฟเส้นแสดงแนวโน้มเหตุการณ์ตามเวลา
   - แผนที่ความร้อน (heatmap) แสดงพื้นที่ที่เกิดเหตุบ่อย

---

## 12. สรุปผลการพัฒนาโปรแกรม

โครงการ "ระบบจัดการเวรและบันทึกเหตุประจำวันของตำรวจ" ได้รับการพัฒนาสำเร็จและสามารถใช้งานได้จริงตามวัตถุประสงค์ที่ตั้งไว้ทั้งหมด โดยมีรายละเอียดสรุปดังนี้

### ความสำเร็จตามวัตถุประสงค์
✅ **พัฒนาระบบจัดการข้อมูลเจ้าหน้าที่** — สามารถ CRUD (เพิ่ม อ่าน แก้ไข ลบ) ได้ครบถ้วน พร้อมตรวจสอบความซ้ำซ้อนของเลขประจำตัว

✅ **พัฒนาระบบจัดตารางเวร** — จัดเวรและติดตามสถานะ (รอเข้าเวร/กำลังปฏิบัติ/เสร็จสิ้น) ได้แบบเรียลไทม์

✅ **สร้างระบบบันทึกเหตุการณ์** — บันทึกเหตุตามประเภท ระดับความรุนแรง และสถานะการดำเนินการได้อย่างเป็นระบบ

✅ **พัฒนาแดชบอร์ดสรุปภาพรวม** — แสดงสถิติและกราฟแท่งที่เข้าใจง่าย ช่วยให้เห็นภาพรวมได้ทันที

✅ **ประยุกต์ใช้ความรู้จากรายวิชา** — นำโครงสร้างการเขียนโปรแกรมที่เรียนมาใช้ครบทุกหัวข้อ (ตัวแปร เงื่อนไข loop ฟังก์ชัน โครงสร้างข้อมูล การจัดการฐานข้อมูล exception handling)

### จุดเด่นของโปรแกรม
1. **ใช้งานได้จริงทั้งในเครื่องและออนไลน์** — รองรับ SQLite (development) และ PostgreSQL (production) ด้วยโค้ดชุดเดียว
2. **ความปลอดภัย** — รหัสผ่านถูกเข้ารหัสด้วย SHA-256 และมีระบบป้องกันหน้าที่ต้องล็อกอินก่อน
3. **ง่ายต่อการใช้งาน** — UI สะอาดตา ไม่ซับซ้อน มีข้อความแจ้งเตือนที่ชัดเจนทุกการกระทำ
4. **พร้อม deploy** — มีไฟล์ `render.yaml`, `Procfile`, `requirements.txt` ครบ สามารถขึ้นออนไลน์ฟรีได้ทันที
5. **โค้ดมีคุณภาพ** — แยกโครงสร้างชัดเจน (app.py = routing, db.py = data layer) มี comment อธิบายทุกส่วน ใช้ฟังก์ชันที่มีชื่อตรงตามหน้าที่

### ประโยชน์ที่ได้รับ
- **ผู้พัฒนา** — ได้ฝึกทักษะการเขียนโปรแกรมจริง ตั้งแต่วิเคราะห์ปัญหา ออกแบบ พัฒนา ทดสอบ และ deploy
- **ผู้ใช้งาน** — ได้ระบบที่ช่วยลดภาระงานเอกสาร ค้นหาข้อมูลได้เร็ว และเห็นภาพรวมได้ชัดเจน
- **องค์กร** — สามารถนำไปปรับใช้จริงในหน่วยงานตำรวจหรือหน่วยงานรักษาความปลอดภัยอื่น ๆ

### บทเรียนที่ได้รับ
1. **การวางแผนโครงสร้างโค้ดตั้งแต่แรก** สำคัญมาก ช่วยให้ขยายฟีเจอร์ใหม่ได้ง่าย
2. **การอ่าน documentation** ของ Flask, SQLite, PostgreSQL ช่วยแก้ปัญหาได้เร็วกว่าลองผิดลองถูก
3. **การทดสอบบ่อย ๆ** ตั้งแต่แต่ละฟีเจอร์ย่อย ทำให้เจอบั๊กเร็วและแก้ไขง่าย
4. **การแยกส่วน Backend (Python) และ Frontend (HTML/CSS)** ทำให้สมาชิกในกลุ่มทำงานพร้อมกันได้โดยไม่ชนกัน

---

## 13. เอกสารอ้างอิง

1. Flask Documentation — https://flask.palletsprojects.com/
2. SQLite Documentation — https://www.sqlite.org/docs.html
3. PostgreSQL Documentation — https://www.postgresql.org/docs/
4. Python Official Documentation — https://docs.python.org/3/
5. Render Documentation — https://render.com/docs
6. W3Schools HTML/CSS Tutorial — https://www.w3schools.com/
7. Real Python Flask Tutorial — https://realpython.com/tutorials/flask/
8. GitHub Documentation — https://docs.github.com/

---

## ภาคผนวก

### A. โครงสร้างไฟล์โปรเจกต์
```
PoliceDutyLog/
├── app.py                  # เว็บแอปหลัก
├── db.py                   # ชั้นจัดการฐานข้อมูล
├── seed_data.py            # สคริปต์เติมข้อมูลตัวอย่าง
├── requirements.txt        # รายการ dependencies
├── Procfile                # คำสั่งรันบนเซิร์ฟเวอร์
├── render.yaml             # ไฟล์ config deploy
├── README.md               # คู่มือการใช้งาน
├── DEPLOY_GUIDE.md         # คู่มือ deploy
├── templates/              # เทมเพลต HTML
│   ├── base.html
│   ├── login.html
│   ├── register.html
│   ├── dashboard.html
│   ├── officers.html
│   ├── duties.html
│   └── incidents.html
└── static/
    └── style.css           # สไตล์ CSS
```

### B. คำสั่งติดตั้งและรัน
```bash
# ติดตั้ง dependencies
pip install -r requirements.txt

# เติมข้อมูลตัวอย่าง (ไม่บังคับ)
python seed_data.py

# รันเว็บแอป
python app.py
```

### C. บัญชีเริ่มต้น
- **Username:** admin
- **Password:** admin123

---

**หมายเหตุ:** รายงานฉบับนี้เป็นส่วนหนึ่งของการส่งงานโครงการรายวิชา 341314 Computer Programming โดยข้อมูลเจ้าหน้าที่และเหตุการณ์ทั้งหมดในระบบเป็นข้อมูลสมมติเพื่อการศึกษาเท่านั้น





